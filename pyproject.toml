[project]
name = "minidl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "minidiff",
    "tqdm>=4.67.1",
]

[tool.uv.sources]
minidiff = { path = "../minidiff" }
